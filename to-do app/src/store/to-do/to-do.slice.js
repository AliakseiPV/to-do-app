import { createSlice } from "@reduxjs/toolkit"

export const todoSlice = createSlice({
	name: 'todo',
	initialState: {
		isLoading: false,
		error: null,
		initialState: []
	},
	redusers: {
		addList: (state) => {

		}
	}

})